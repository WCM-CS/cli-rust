# cli-rust

Rust CLI meal generator tool. User inputs macro nutrient values and the tool generates a meal to fit the desires values. Makes a call to proxy server to interact with the USDA API and grabs the food data based on the FDC ID's. After values aare obtained the tool scales the amounts accordingly. 
